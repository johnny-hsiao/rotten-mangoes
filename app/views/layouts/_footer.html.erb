<footer>
  <small>
    <% if current_user %>
      Signed in as <%= current_user.full_name %> ( <%= link_to "Log Out", session_path("current"), method: :delete %> <%= link_to "Admin Page", admin_users_path if current_user.is_admin? %>)
    <% else %>
      <%= link_to "Log In", new_session_path %> | <%= link_to "Sign Up", new_user_path %>
    <% end %>
  </small>
</footer>